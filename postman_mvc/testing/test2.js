var expect = require('chai').assert;
var assert = require('chai').expect;
var valid = require('../mvc/utils/validation.util');

describe("Validation for Number",()=>{
   it('message1',(getData)=>{
       expect(value).to.equal('hello');
   })
});